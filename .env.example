FAL_KEY=
RENDER_API_KEY=

# Convex (optional). `npx convex dev` writes these to `.env.local`.
# Leave VITE_CONVEX_URL empty to keep the leaderboard on this device only.
VITE_CONVEX_URL=
# CONVEX_DEPLOYMENT=

